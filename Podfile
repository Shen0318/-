# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target '第三方库' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'Masonry'
  # Pods for 第三方库

  target '第三方库Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target '第三方库UITests' do
    # Pods for testing
  end

end
